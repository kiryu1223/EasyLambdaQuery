package com.easy.query.lambda.util;

public class QueryParser
{
    private QueryParser()
    {
    }

//    public static void parser(ClientQueryable<?> queryable, QueryData queryData)
//    {
//        // 用不着
////        List<QueryData> queryDataList = queryData.getQueryDataList();
////
////        if (!queryDataList.isEmpty())
////        {
////            for (QueryData data : queryDataList)
////            {
////                parser(queryable, data);
////            }
////        }
//
//        for (Criteria criterion : queryData.getCriteria())
//        {
//            criterion.analysis(queryable,queryData);
//        }
//    }
}
